# LCD display graphics testing
yep

## pio nvim setup
1. `$ git clone https://github.com/ironlungx/nvim-pio`
2. edit `platformio.ini` to set `board` and `platform`
3. `$ pio run -t clean && pio run -t compiledb`
